export default (base) => ({
  [`${base}`]: `${base}`,
  [`${base}_REQUEST_PENDING`]: `${base}_REQUEST_PENDING`,
  [`${base}_REQUEST_SUCCESS`]: `${base}_REQUEST_SUCCESS`,
  [`${base}_REQUEST_FAILURE`]: `${base}_REQUEST_FAILURE`
})
